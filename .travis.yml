language: python
python: 3.6
cache: pip

install:
  - pip install codecov pytest-travis-fold
  - pip install -r monitoring/requirements-dev.txt

script:
  - make lint
  - pytest -v --cov=./
  - codecov
